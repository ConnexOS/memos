# MEMOS v0.5.1 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 发布 MEMOS v0.5.1「统一服务器」到 GitHub Release + PyPI

**Architecture:** 双仓发布流程。本地开发仓库（D:/DevSpace/MEMOS）完成回归测试→提交→打 tag，通过 rsync 同步到过渡仓库（D:/MyGitHub/MEMOS），经脱敏处理后再推送到 GitHub 远程仓库。GitHub Actions CI 通过后创建 Release，最后发布到 PyPI。

**Tech Stack:** git, pytest, ruff, rsync, gh CLI, python build/twine

**参考文档:** `document/51版本/MEMOS-v0.5.1-版本发布方案.md`

---

### Task 0: 最终回归测试

**文件:**
- 运行: 全量测试

- [ ] **Step 1: 运行全量 pytest**

```bash
cd D:/DevSpace/MEMOS && ./venv/Scripts/python -m pytest tests/ -v -q 2>&1 | tail -20
```

预期: `N passed, X skipped`，无 FAILED

- [ ] **Step 2: 运行 ruff check**

```bash
cd D:/DevSpace/MEMOS && ./venv/Scripts/python -m ruff check src/
```

预期: 零告警

- [ ] **Step 3: CI 发行检查**

```bash
cd D:/DevSpace/MEMOS && ./venv/Scripts/python -m ruff format --check src/ 2>&1
```

预期: `N file(s) left unchanged`

---

### Task 1: 更新 CHANGELOG + 提交 README

**文件:**
- 修改: `CHANGELOG.md`
- 已修改: `README.md`, `README.zh.md`

- [ ] **Step 1: 更新 CHANGELOG.md**

追加 v0.5.1 发布记录到 CHANGELOG.md 顶部：

```markdown
## [0.5.1] - 2026-06-09

### 统一服务器架构
- 单进程 FastAPI 统一服务（MCP SSE + Dashboard + Hook 三合一）
- SSE 直连协议，移除 MCP 薄代理
- SessionAuthStore session→token 映射解决 SSE 无自定义 Header 问题
- ProjectAwareSSEWrapper 从 URL 提取 project_id + token

### 身份与安全
- 多用户 Token 认证系统（bcrypt + users.json）
- Dashboard login/logout 页面
- 首次启动自动创建 admin 用户

### 数据隔离
- creator_id + scope 双字段用户级数据隔离
- 3 种隔离级别：team / personal / 混合

### 客户端轻量化
- 依赖拆分为默认 + [server] extras（默认 ~3MB，零 ML 依赖）
- `memos setup` 一键配置命令（login + mcp + hook 合并）
- `.memos-project` JSON 单一数据源，移除三条兜底链
- `__init__.py` 清理顶层重导入，重型模块惰性加载

### CLI 增强
- `memos setup`：一键初始化客户端
- `memos server`：启动统一服务
- `memos user add/list/remove/token-regen`：用户管理
- `memos migrate --to-unified`：v0.4.x 升级迁移入口

### 开发基础设施
- contextvars 替代 threading.local，异步安全
- E2E 测试框架 + 数据隔离测试
- `scripts/release_exclude.txt` 发布排除清单
```

- [ ] **Step 2: 验证文件状态**

```bash
cd D:/DevSpace/MEMOS && git status
```

预期: 显示 README.md、README.zh.md、CHANGELOG.md、scripts/release_exclude.txt 等为 modified

---

### Task 2: 本地提交 + 打标签

- [ ] **Step 1: git add + commit**

```bash
cd D:/DevSpace/MEMOS && git add -A && git commit -m "release: v0.5.1"
```

预期: commit 成功，输出 commit hash

- [ ] **Step 2: 打 tag**

```bash
cd D:/DevSpace/MEMOS && git tag -a v0.5.1 -m "MEMOS v0.5.1 统一服务器"
```

- [ ] **Step 3: 验证**

```bash
cd D:/DevSpace/MEMOS && git log --oneline -3 && echo "---" && git tag | tail -3
```

预期: 显示新 commit + v0.5.1 tag

---

### Task 3: 双仓库同步 + 脱敏

- [ ] **Step 1: rsync 同步到过渡仓库**

```bash
rsync -av --exclude-from=scripts/release_exclude.txt /d/DevSpace/MEMOS/ /d/MyGitHub/MEMOS/
```

预期: 文件传输正常，排除清单生效

- [ ] **Step 2: 脱敏检查**

```bash
# 检查敏感文件
find D:/MyGitHub/MEMOS -name "*credentials*" -o -name "*secret*" -o -name ".mcp.json" 2>/dev/null
# 检查本地路径泄露
grep -r "D:/DevSpace" D:/MyGitHub/MEMOS/src/ --include="*.py" 2>/dev/null || echo "无本地路径泄露"
```

预期: 无敏感文件，无本地路径泄露

- [ ] **Step 3: 过渡仓库提交**

```bash
git -C /d/MyGitHub/MEMOS add -A && git -C /d/MyGitHub/MEMOS commit -m "release: v0.5.1"
```

- [ ] **Step 4: 推送到 GitHub**

```bash
git -C /d/MyGitHub/MEMOS push origin main --tags
```

预期: push 成功，远程仓库更新

---

### Task 4: GitHub Release + PyPI 发布

- [ ] **Step 1: 等待 GitHub Actions CI 完成**

在浏览器或 CLI 中确认 CI pipelines 全部绿色。

- [ ] **Step 2: 创建 GitHub Release**

```bash
gh release create v0.5.1 --title "MEMOS v0.5.1「统一服务器」" --notes-file scripts/release_notes.md
```

如 `scripts/release_notes.md` 不存在，可用内联方式：

```bash
gh release create v0.5.1 --title "MEMOS v0.5.1「统一服务器」" --notes "见 CHANGELOG.md"
```

- [ ] **Step 3: 构建分发包**

```bash
cd D:/DevSpace/MEMOS && ./venv/Scripts/python -m build
```

预期: dist/ 目录生成 .tar.gz 和 .whl 文件

- [ ] **Step 4: 发布到 PyPI**

```bash
cd D:/DevSpace/MEMOS && ./venv/Scripts/python -m twine upload dist/* -u __token__ -p <PYPI_TOKEN>
```

预期: upload 成功，PyPI 页面可见

- [ ] **Step 5: 清理构建产物**

```bash
cd D:/DevSpace/MEMOS && rm -rf dist/ && rm -rf *.egg-info
```

---

### Task 5: 安装验证

- [ ] **Step 1: pip install 验证**

```bash
pip install memomate==0.5.1
```

预期: 安装成功（轻量模式，无 ML 依赖）

- [ ] **Step 2: CLI 验证**

```bash
memos doctor
memos status
```

预期: 版本号显示 v0.5.1，诊断通过

- [ ] **Step 3: 服务端安装验证**

```bash
pip install "memomate[server]"
memos server --help
```

预期: server 命令帮助正常显示

---

### Task 6: 发布后清理

- [ ] **Step 1: 切回本地 master**

```bash
cd /d/DevSpace/MEMOS && git checkout master
```

- [ ] **Step 2: 验证本地与远程一致**

```bash
cd /d/DevSpace/MEMOS && git log --oneline -1 && git diff v0.5.1 HEAD --stat
```

预期: 本地 HEAD 与 tag 一致或领先（无未提交变更）

---

**发布完成。** 如 PyPI 发布失败，降级方案为仅 GitHub Release 发布，用户通过 `pip install git+https://github.com/laofisher/memos.git@v0.5.1` 安装。
